# dependencies (only needed to re-generate the helix frames)
node_modules/

# local tooling / os
.DS_Store
*.log
.claude/
.netlify/
.vscode/

# source masters (kept out of the repo — frames are committed instead)
*.mp4
# ...but the web-optimized hero loop video IS shipped
!assets/hero-loop.mp4
